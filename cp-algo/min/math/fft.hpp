#ifndef CP_ALGO_MATH_FFT_HPP
#define CP_ALGO_MATH_FFT_HPP
#include "dft.hpp"
#include <cstring>
#include <tuple>
CP_ALGO_SIMD_PRAGMA_PUSH
namespace cp_algo::math::fft{void mul_slow(auto&a,auto const&b,size_t k){if(!std::empty(a)&&std::data(a)==std::data(b)){using base=std::decay_t<decltype(a[0])>;size_t n=std::min(k,std::size(a)),m=std::min(k,std::size(b));if(!m){a.clear();return;}a.resize(k);for(size_t j=k;j-->0;){base sum=0;size_t lo=j>=n?j+1-n:0,hi=std::min(j+1,m);for(size_t i=lo;i<hi;i++){if(n==m&&i>j-i){break;}auto term=a[i]*a[j-i];sum+=n==m&&i!=j-i?term+term:term;}a[j]=sum;}return;}if(std::empty(a)||std::empty(b)){a.clear();}else{size_t n=std::min(k,std::size(a));size_t m=std::min(k,std::size(b));a.resize(k);for(int j=int(k-1);j>=0;j--){a[j]*=b[0];for(int i=std::max(j-(int)n,0)+1;i<std::min(j+1,(int)m);i++){a[j]+=a[j-i]*b[i];}}}}size_t com_size(size_t as,size_t bs){if(!as||!bs){return 0;}return std::max(flen,std::bit_ceil(as+bs-1)/2);}void mul_truncate(auto&a,auto const&b,size_t k){using base=std::decay_t<decltype(a[0])>;if(std::min({k,std::size(a),std::size(b)})<magic){mul_slow(a,b,k);return;}auto n=std::max(flen,std::bit_ceil(std::min(k,std::size(a))+std::min(k,std::size(b))-1)/2);size_t as=std::min(k,std::size(a)),bs=std::min(k,std::size(b));size_t tail=as+bs-1-n;if(tail<=32&&as<=n&&bs<=n){std::array<base,32>high{};for(size_t i=0;i<tail;i++){for(size_t j=n+i-bs+1;j<as;j++){high[i]+=a[j]*b[n+i-j];}}auto A=dft<base>(a|std::views::take(k),n/2);if(as==bs&&std::data(a)==std::data(b)){a.resize((k+flen-1)/flen*flen);A.mul(A,a,std::min(k,n));}else{auto B=dft<base>(b|std::views::take(k),n/2);a.resize((k+flen-1)/flen*flen);A.mul_inplace(B,a,std::min(k,n));}auto wrap=bpow(dft<base>::factor,n);for(size_t i=0;i<tail;i++){a[i]+=wrap*high[i];if(n+i<k){a[n+i]=high[i];}}a.resize(k);return;}auto A=dft<base>(a|std::views::take(k),n);if(as==bs&&std::data(a)==std::data(b)){a.resize((k+flen-1)/flen*flen);A.mul(A,a,k);}else{auto B=dft<base>(b|std::views::take(k),n);a.resize((k+flen-1)/flen*flen);A.mul_inplace(B,a,k);}a.resize(k);}template<bool inverse=false>void mod_split(auto&&x,size_t n,auto k){using base=std::decay_t<decltype(k)>;dft<base>::init();assert(std::size(x)==2*n);u64x4 cur=u64x4{}+(k*bpow(base(2),32)).getr();for(size_t i=0;i<n;i+=flen){u64x4 xl={x[i].getr(),x[i+1].getr(),x[i+2].getr(),x[i+3].getr()};u64x4 xr={x[n+i].getr(),x[n+i+1].getr(),x[n+i+2].getr(),x[n+i+3].getr()};if constexpr(!inverse){xr=montgomery_mul(xr,cur,dft<base>::mod,dft<base>::imod);xr=xr>=base::mod()?xr-base::mod():xr;}auto t=xr;xr=xl-t;xl+=t;xl=xl>=base::mod()?xl-base::mod():xl;xr=xr>=base::mod()?xr+base::mod():xr;if constexpr(inverse){xl=(xl+(xl&1)*base::mod())>>1;xr=montgomery_mul(xr,cur,dft<base>::mod,dft<base>::imod);xr=xr>=base::mod()?xr-base::mod():xr;}for(size_t k=0;k<flen;k++){x[i+k].setr(typename base::UInt(xl[k]));x[n+i+k].setr(typename base::UInt(xr[k]));}}cp_algo::checkpoint(inverse?"mod join":"mod split");}void cyclic_mul(auto&a,auto&&b,size_t k,bool zero_upper=false){assert(std::popcount(k)==1);assert(std::size(a)==std::size(b)&&std::size(a)==k);using base=std::decay_t<decltype(a[0])>;dft<base>::init();bool square=std::data(a)==std::data(b);if(k<=(1<<16)){big_vector<base>ap(begin(a),end(a));if(square){mul_truncate(ap,ap,2*k);}else{mul_truncate(ap,b,2*k);}mod_split(ap,k,bpow(dft<base>::factor,k));std::ranges::copy(ap|std::views::take(k),begin(a));return;}k/=2;auto factor=bpow(dft<base>::factor,k);if(zero_upper){std::ranges::copy(std::span(a).first(k),begin(a)+k);if(!square){std::ranges::copy(std::span(b).first(k),begin(b)+k);}}else{mod_split(a,k,factor);if(!square){mod_split(b,k,factor);}}auto la=std::span(a).first(k);auto lb=std::span(b).first(k);auto ra=std::span(a).last(k);auto rb=std::span(b).last(k);cyclic_mul(la,lb,k);auto A=dft<base>(ra,k/2);if(square){A.mul(A,ra,k);}else{auto B=dft<base>(rb,k/2);A.mul_inplace(B,ra,k);}base i2=base(2).inv();factor=factor.inv()*i2;mod_split<true>(a,k,factor);}auto make_copy(auto&&x){return x;}void cyclic_mul(auto&a,auto const&b,size_t k){return cyclic_mul(a,make_copy(b),k);}template<modint_type base>struct gaussian{static inline bool ready=false,available=false;static inline uint32_t a=0,b=0,root=0;static void init(){if(ready){return;}ready=true;uint64_t p=base::mod();if(p%4!=1||p>=(uint64_t(1)<<31)){return;}uint64_t s=0;for(uint64_t g=2;g<p;g++){if(bpow(base(g),(p-1)/2)!=base(1)){s=bpow(base(g),(p-1)/4).getr();break;}}uint64_t r0=p,r1=s;while(r1*r1>p){std::tie(r0,r1)=std::pair{r1,r0%r1};}uint64_t aa=r1,bb=uint64_t(std::sqrt((long double)(p-aa*aa))+0.5);if(aa*aa+bb*bb!=p){return;}a=uint32_t(aa);b=uint32_t(bb);root=(base(b)/base(a)).getr();assert(base(root)*base(root)==base(p-1));available=true;}static bool usable(size_t as,size_t bs){init();if(!available||!as||!bs){return false;}size_t n=std::max(flen,std::bit_ceil(as+bs-1)/2);return std::max(as,bs)<=n&&n>=(size_t(1)<<18);}struct lattice{double a,b,root,inv_b,a_over_p,b_over_p;lattice():a(gaussian::a),b(gaussian::b),root(gaussian::root),inv_b(1.0/b),a_over_p(a/double(base::mod())),b_over_p(b/double(base::mod())){}};static u32x4 project(vpoint value,bool negative,lattice const&L){constexpr double p=base::mod();auto R=round(real(value)),I=round(negative?-imag(value):imag(value));auto q=round(I*L.inv_b);auto U=vftype(_mm256_fnmadd_pd(__m256d(q),_mm256_set1_pd(L.a),__m256d(R)));auto V=vftype(_mm256_fnmadd_pd(__m256d(q),_mm256_set1_pd(L.b),__m256d(I)));auto h=vftype(_mm256_fmadd_pd(__m256d(V),_mm256_set1_pd(L.root),__m256d(U)));q=round(h*(1.0/p));auto out=vftype(_mm256_fnmadd_pd(__m256d(q),_mm256_set1_pd(p),__m256d(h)));out=out<0?out+p:out;return u32x4(_mm256_cvttpd_epi32(__m256d(out)));}template<bool stream>static void fill(cvector&c,auto const&x,size_t n,bool negative,u64x4 state){const lattice L;auto const*src=reinterpret_cast<const uint32_t*>(std::data(x));size_t count=std::size(x);c.r.resize(n/flen);auto*dst=reinterpret_cast<double*>(c.r.data());u32x8 words{};auto emit=[&](size_t i,i32x4 bits)__attribute__((always_inline)){auto v=__builtin_convertvector(bits,vftype);if(i%8==0){state^=state<<13;state^=state>>7;state^=state<<17;words=u32x8(state);}i32x4 small=i%8?i32x4(__builtin_shufflevector(words,words,1,3,5,7)):i32x4(__builtin_shufflevector(words,words,0,2,4,6));auto noise=__builtin_convertvector(small,vftype)*0x1p-32;auto q=round(v*L.a_over_p+noise),t=round(v*L.b_over_p+noise);auto re=v-q*L.a-t*L.b,im=t*L.a-q*L.b;if constexpr(stream){_mm256_stream_pd(dst+2*i,__m256d(re));_mm256_stream_pd(dst+2*i+flen,__m256d(negative?-im:im));}else{c.r[i/flen]=vpoint{re,negative?-im:im};}};size_t full=count/flen*flen;for(size_t i=0;i<full;i+=flen){i32x4 bits;std::memcpy(&bits,src+i,sizeof(bits));emit(i,bits);}if(full<count){i32x4 bits{};for(size_t j=full;j<count;j++){bits[j-full]=int32_t(src[j]);}emit(full,bits);}if constexpr(stream){_mm_sfence();}std::fill(c.r.begin()+(count+flen-1)/flen,c.r.end(),vpoint{});checkpoint("gaussian init");if(n!=(1<<24)){c.fft();}}static void mul(auto&a,auto const&b){static_assert(sizeof(std::decay_t<decltype(a[0])>)==4);size_t as=std::size(a),bs=std::size(b),need=as+bs-1;size_t n=std::max(flen,std::bit_ceil(need)/2);assert(available&&as<=n&&bs<=n);using D=dft<base>;D::init();base r32=bpow(base(2),32);auto highmul=u32x8{}+uint32_t(((base(2)*base(root)).inv()*r32).getr());auto seed=[](){return u64x4{random::rng()|1,random::rng()|1,random::rng()|1,random::rng()|1};};u64x4 seed_a=seed(),seed_b=seed();const lattice L;a.resize(2*n);cvector A(0),B(0);auto*out=reinterpret_cast<uint32_t*>(std::data(a));for(bool negative:{false,true}){if(n==(1<<24)){if constexpr(cvector::fuse_forward){cvector::fuse_args fa{out,as,seed_a[0],double(gaussian::a),double(gaussian::b),gaussian::a/double(base::mod()),gaussian::b/double(base::mod())};cvector::fuse_args fb{reinterpret_cast<const uint32_t*>(std::data(b)),bs,seed_b[0],fa.a,fa.b,fa.a_over_p,fa.b_over_p};if(negative){A.template cache_product<true>(B,fa,fb);}else{A.template cache_product<false>(B,fa,fb);}}else{fill<true>(A,std::span(a).first(as),n,negative,seed_a);fill<true>(B,b,n,negative,seed_b);if(negative){A.template cache_product<true>(B);}else{A.template cache_product<false>(B);}}}else{fill<false>(A,std::span(a).first(as),n,negative,seed_a);fill<false>(B,b,n,negative,seed_b);A.dot(B);A.template ifft<true,false>();}using i32x8=simd<int32_t,8>;auto scale=vz+double(flen)/double(n);for(size_t i=0;i<n;i+=8){auto sum0=project(A.at(i)*scale,negative,L);auto sum1=project(A.at(i+4)*scale,negative,L);auto sum=__builtin_shufflevector(sum0,sum1,0,1,2,3,4,5,6,7);if(negative){u32x8 plus;std::memcpy(&plus,out+n+i,sizeof(plus));auto lo=plus+sum;lo=i32x8(lo)>=int32_t(base::mod())?lo-base::mod():lo;lo=(lo+(lo&1)*base::mod())>>1;auto hi=montgomery_mul(plus+base::mod()-sum,highmul,D::mod,D::imod);hi=i32x8(hi)>=int32_t(base::mod())?hi-base::mod():hi;std::memcpy(out+i,&lo,sizeof(lo));std::memcpy(out+n+i,&hi,sizeof(hi));}else{std::memcpy(out+n+i,&sum,sizeof(sum));}}checkpoint("gaussian recover");}a.resize(need);}};namespace impl{void mul_unbalanced(auto&a,auto const&b){using base=std::decay_t<decltype(a[0])>;auto x=std::span<base const>(a),y=std::span<base const>(b);if(x.size()<y.size()){std::swap(x,y);}constexpr size_t length=1<<15;size_t step=length-y.size()+1;auto fixed=dft<base>(y,length/2);std::decay_t<decltype(a)>result(x.size()+y.size()-1);big_vector<base>work(length);for(size_t start=0;start<x.size();start+=step){size_t count=std::min(step,x.size()-start);auto block=dft<base>(x.subspan(start,count),length/2);size_t need=count+y.size()-1;block.mul(fixed,work,need);for(size_t i=0;i<need;i++){result[start+i]+=work[i];}}a=std::move(result);}}void mul(auto&a,auto&&b){if(std::empty(a)||std::empty(b)){a.clear();return;}bool square=std::data(a)==std::data(b)&&std::size(a)==std::size(b);if(!square&&std::data(a)==std::data(b)){auto copy=make_copy(b);return mul(a,copy);}using base=std::decay_t<decltype(a[0])>;if constexpr(sizeof(base)==4){if(gaussian<base>::usable(std::size(a),std::size(b))){if(square){auto copy=make_copy(b);return gaussian<base>::mul(a,copy);}return gaussian<base>::mul(a,b);}}size_t small=std::min(size(a),size(b)),large=std::max(size(a),size(b));if(small>=magic&&small<=4096&&large>=(1<<20)&&large/small>=64){return impl::mul_unbalanced(a,b);}size_t N=size(a)+size(b);if(N>(1<<20)){N--;size_t NN=std::bit_ceil(N);bool zero_upper=std::max(size(a),size(b))<=NN/2;a.resize(NN);if(zero_upper&&!square){size_t half=NN/2;b.resize(half);auto lo=std::span(a).first(half),hi=std::span(a).last(half);{auto A=dft<base>(lo,half/2);auto B=dft<base>(b,half/2);A.mul_inplace(B,hi,half);}cyclic_mul(lo,b,half);mod_split<true>(a,half,(base(2)*bpow(dft<base>::factor,half)).inv());}else{if(!square){b.resize(NN);}cyclic_mul(a,b,NN,zero_upper);}a.resize(N);}else{mul_truncate(a,b,N-1);}}void mul(auto&a,auto const&b){if(std::empty(a)||std::empty(b)){a.clear();return;}size_t small=std::min(size(a),size(b)),large=std::max(size(a),size(b));if(small>=magic&&small<=4096&&large>=(1<<20)&&large/small>=64){return impl::mul_unbalanced(a,b);}using base=std::decay_t<decltype(a[0])>;if constexpr(sizeof(base)==4){if(gaussian<base>::usable(std::size(a),std::size(b))){if(std::data(a)==std::data(b)){auto copy=make_copy(b);return gaussian<base>::mul(a,copy);}return gaussian<base>::mul(a,b);}}size_t N=size(a)+size(b);if(N>(1<<20)){if(std::data(a)==std::data(b)&&std::size(a)==std::size(b)){mul(a,a);}else{mul(a,make_copy(b));}}else{mul_truncate(a,b,N-1);}}}
#pragma GCC pop_options
#endif
