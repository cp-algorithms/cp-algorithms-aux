#ifndef CP_ALGO_LINALG_MATRIX_HPP
#define CP_ALGO_LINALG_MATRIX_HPP
#include "../random/rng.hpp"
#include "../math/common.hpp"
#include "vector.hpp"
#include "strassen.hpp"
#include <iostream>
#include <optional>
#include <cassert>
#include <vector>
#include <array>
CP_ALGO_SIMD_PRAGMA_PUSH
namespace cp_algo::linalg{ enum gauss_mode{normal,reverse}; template<typename base_t,class _vec_t=std::conditional_t< math::modint_type<base_t>, modint_vec<base_t>, vec<base_t>>> struct matrix:big_vector<_vec_t>{ using vec_t=_vec_t; using base=base_t; using Base=big_vector<vec_t>; using Base::Base; matrix(size_t n):matrix(n,n){} matrix(size_t n,size_t m){ Base::reserve(n); for(size_t i=0;i<n;i++)Base::emplace_back(m); } matrix(Base const&t):Base(t){} matrix(Base&&t):Base(std::move(t)){} template<std::ranges::input_range R> matrix(R&&r):Base(r.begin(),r.end()){} size_t n()const{return size(*this);} size_t m()const{return n()?size(row(0)):0;} void resize(size_t n,size_t m){ Base::resize(n); for(auto&it: *this){ it.resize(m); } } auto&row(size_t i){return(*this)[i];} auto const&row(size_t i)const{return(*this)[i];} auto elements(){return*this|std::views::join;} auto elements()const{return*this|std::views::join;} matrix operator-()const{ return*this|std::views::transform([](auto const&x){return vec_t(-x);}); } matrix&operator+=(matrix const&t){ for(auto[a,b]:std::views::zip(*this,t)){ for(auto[x,y]:std::views::zip(a,b))x+=y; } return*this; } matrix&operator-=(matrix const&t){ for(auto[a,b]:std::views::zip(*this,t)){ for(auto[x,y]:std::views::zip(a,b))x-=y; } return*this; } matrix operator+(matrix const&t)const{return matrix(*this)+=t;} matrix operator-(matrix const&t)const{return matrix(*this)-=t;} matrix&operator*=(base t){for(auto&it: *this)it*=t;return*this;} matrix operator*(base t)const{return matrix(*this)*=t;} matrix&operator/=(base t){return*this*=base(1)/t;} matrix operator/(base t)const{return matrix(*this)/=t;} matrix&operator*=(matrix const&t){return*this= *this*t;} void read_transposed(){ for(size_t j=0;j<m();j++){ for(size_t i=0;i<n();i++){ std::cin>>(*this)[i][j]; } } } void read(){ for(auto&it: *this){ it.read(); } } void print()const{ for(auto const&it: *this){ it.print(); } } static matrix block_diagonal(big_vector<matrix>const&blocks){ size_t n=0; for(auto&it:blocks){ assert(it.n()==it.m()); n+=it.n(); } matrix res(n); n=0; for(auto&it:blocks){ for(size_t i=0;i<it.n();i++){ std::ranges::copy(it[i],begin(res[n+i])+n); } n+=it.n(); } return res; } static matrix random(size_t n,size_t m){ matrix res(n,m); std::ranges::generate(res,std::bind(vec_t::random,m)); return res; } static matrix random(size_t n){ return random(n,n); } static matrix eye(size_t n){ matrix res(n); for(size_t i=0;i<n;i++){ res[i][i] =1; } return res; } matrix operator|(matrix const&b)const{ assert(n()==b.n()); matrix res(n(),0); for(size_t i=0;i<n();i++){ res[i] =row(i)|b[i]; } return res; } void assign_submatrix(auto viewx,auto viewy,matrix const&t){ for(auto[a,b]:std::views::zip(*this|viewx,t)){ std::ranges::copy(b,begin(a|viewy)); } } auto submatrix(auto viewx,auto viewy)const{ return*this|viewx|std::views::transform([viewy](auto const&y){ return y|viewy; }); } matrix T()const{ matrix res(m(),n()); constexpr size_t block=128; for(size_t first=0;first<m();first+=block){ size_t last=std::min(first+block,m()); for(size_t i=0;i<n();i++){ auto const&src=row(i); for(size_t j=first;j<last;j++)res[j][i] =src[j]; } } return res; } matrix operator*(matrix const&b)const{ assert(m()==b.n()); if constexpr(impl::use_strassen<vec_t>){ if(std::min({n(),m(),b.m()})>=(base::bits<=32?64:512)){ return impl::strassen_product<base::mod()>::product(*this,b); } } matrix res(n(),b.m()); if constexpr(requires{requires vec_t::use_simd;}){ if(n()==1){ res[0] =b.apply(row(0)); return res; } } constexpr size_t block=32; for(size_t first=0;first<m();first+=block){ size_t last=std::min(first+block,m()); size_t i=0; for(;i+1<n();i+=2){ size_t j=first; if constexpr(requires{requires vec_t::use_simd;}){ constexpr size_t batch=vec_t::batch_size; if(b.m()>=8)for(;j+batch<=last;j+=batch){ std::array<typename vec_t::Base const*,batch>src; std::array<base,2*batch>c; for(size_t t=0;t<batch;t++){ src[t] = &b[j+t]; c[t] =row(i)[j+t];c[batch+t] =row(i+1)[j+t]; } vec_t::template add_scaled_batch<batch,2>({&res[i],&res[i+1]},src,c); } } for(;j+1<last;j+=2){ add_scaled_pair(res[i],res[i+1],b[j],b[j+1], {row(i)[j],row(i)[j+1],row(i+1)[j],row(i+1)[j+1]}); } if(j<last){ res[i].add_scaled(b[j],row(i)[j]); res[i+1].add_scaled(b[j],row(i+1)[j]); } } for(;i<n();i++){ for(size_t j=first;j<last;j++){ res[i].add_scaled(b[j],row(i)[j]); } } } res.normalize(); return res; } vec_t apply(vec_t const&x)const{ assert(x.size()==n()); vec_t res(m()); size_t i=0; if constexpr(requires{requires vec_t::use_simd;}){ constexpr size_t batch=vec_t::batch_size; for(;i+batch<=n();i+=batch){ std::array<typename vec_t::Base const*,batch>src; std::array<base,batch>c; for(size_t t=0;t<batch;t++){src[t] = &row(i+t);c[t] =x[i+t];} vec_t::template add_scaled_batch<batch,1>({&res},src,c); } } for(;i<n();i++)res.add_scaled(row(i),x[i]); res.normalize(); return res; } matrix pow(uint64_t k)const{ assert(n()==m()); return bpow<3>(*this,k,eye(n())); } matrix&normalize(){ for(auto&it: *this){ it.normalize(); } return*this; } template<gauss_mode mode=normal> void eliminate(size_t i,size_t k){ auto kinv=base(1)/row(i).normalize()[k]; for(size_t j=(mode==normal)*i;j<n();j++){ if(j!=i){ row(j).add_scaled(row(i),-row(j).normalize(k)*kinv); } } } template<gauss_mode mode=normal> void eliminate(size_t i){ row(i).normalize(); for(size_t j=(mode==normal)*i;j<n();j++){ if(j!=i){ row(j).reduce_by(row(i)); } } } template<gauss_mode mode=normal> matrix&gauss(){ return gauss_to<mode>(m()); } private: template<gauss_mode mode> matrix&gauss_to(size_t lim){ constexpr size_t block=32; for(size_t first=0;first<n();first+=block){ size_t last=std::min(first+block,n()); for(size_t i=first;i<last;i++){ row(i).normalize(); if(row(i).find_pivot().first>=lim)continue; for(size_t j=mode==normal?i+1:first;j<last;j++){ if(j!=i)row(j).reduce_by(row(i)); } } if constexpr(mode==reverse){ for(size_t i=first;i<last;i++)row(i).normalize(); } for(size_t j=mode==normal?last:0;j<n();j++){ if(j>=first&&j<last)continue; bool pair=j+1<n()&&j+1!=first; size_t i=first; if constexpr(requires{requires vec_t::use_simd;}){ constexpr size_t batch=vec_t::batch_size; if(pair)for(;i+batch<=last;i+=batch)reduce_batch<mode,batch>(j,i,lim); } if(pair)for(;i+1<last;i+=2)reduce_pair<mode>(j,i,lim); for(;i<last;i++){ if(row(i).find_pivot().first>=lim)continue; row(j).reduce_by(row(i)); if(pair)row(j+1).reduce_by(row(i)); } j+=pair; } } return mode==reverse?normalize(): *this; } public: template<gauss_mode mode=normal> auto echelonize(size_t lim){ return gauss<mode>().sort_classify(lim); } template<gauss_mode mode=normal> auto echelonize(){ return echelonize<mode>(m()); } size_t rank()const{ auto A=n()>m()?T(): *this; A.gauss(); return std::ranges::count_if(A,[&](auto&row){ return row.find_pivot().first<A.m(); }); } base det()const{ assert(n()==m()); matrix b= *this; b.gauss(); base res=1; for(size_t i=0;i<n();i++){ for(size_t p=b[i].find_pivot().first;p!=i;p=b[i].find_pivot().first){ if(p==n())return 0; std::swap(b[i],b[p]); res= -res; } res*=b[i][i]; } return res; } base pfaffian()const{ assert(n()==m()&&n()%2==0); matrix b= *this; base res=1; for(size_t i=1;i<n();i++){ for(size_t j=i+1;j<n()&&b[i].normalize(i-1)==base(0);j++){ if(b[j].normalize(i-1)!=base(0)){ std::swap(b[i],b[j]); for(size_t k=i;k<n();k++){ std::swap(b[k][i],b[k][j]); } res= -res; } } b[i].normalize(); if(i%2){ res*= -b[i][i-1]; if(res==base(0))return res; } if(b[i][i-1] ==base(0))continue; base inv=base(1)/b[i][i-1]; for(size_t j=i+1;j<n();j++){ b[j].add_scaled(b[i],-b[j].normalize(i-1)*inv,i); } } return res; } std::pair<base,matrix>inv()const{ assert(n()==m()); matrix b(n(),2*n()); for(size_t i=0;i<n();i++){ std::ranges::copy(row(i),begin(b[i])); b[i][n()+i] =1; } auto[pivots,free] =b.template gauss_to<normal>(n()).sort_classify(n()); if(size(pivots)<n()){ return{0,{}}; } base det=1; for(size_t i=0;i<n();i++){ det*=b[i][i]; } matrix res=b.submatrix(std::views::all,std::views::drop(n())); back_substitute(b,pivots,res); return{det,std::move(res)}; } auto kernel()const{ return kernel_of(*this); } std::optional<std::array<matrix,2>>solve(matrix const&t)const{ matrix sols=kernel_of(*this|t); if(sols.n()<t.m()||matrix(sols.submatrix( std::views::drop(sols.n()-t.m()), std::views::drop(m()) ))!= -eye(t.m())){ return std::nullopt; }else{ for(auto&row:sols)row.resize(m()); auto first=begin(sols)+(sols.n()-t.m()); matrix sol(std::make_move_iterator(first),std::make_move_iterator(end(sols))); sols.erase(first,end(sols)); return std::array{std::move(sol),std::move(sols)}; } } auto sort_classify(size_t lim){ size_t rk=0; big_vector<size_t>free,pivots; for(size_t j=0;j<lim;j++){ for(size_t i=rk+1;i<n()&&row(rk)[j] ==base(0);i++){ if(row(i)[j] !=base(0)){ std::swap(row(i),row(rk)); row(rk)= -row(rk); } } if(rk<n()&&row(rk)[j] !=base(0)){ pivots.push_back(j); rk++; }else{ free.push_back(j); } } return std::array{std::move(pivots),std::move(free)}; } private: static matrix kernel_of(matrix A){ auto[pivots,free] =A.echelonize(); matrix rhs(size(pivots),size(free)); for(size_t i=0;i<size(pivots);i++){ for(size_t j=0;j<size(free);j++){ rhs[i][j] =A[i][free[j]]; } } back_substitute(A,pivots,rhs); matrix sols(size(free),A.m()); for(size_t j=0;j<size(pivots);j++){ for(size_t i=0;i<size(free);i++){ sols[i][pivots[j]] =rhs[j][i]; } } for(size_t i=0;i<size(free);i++){ sols[i][free[i]] = -1; } return sols; } static void back_substitute(matrix const&A,auto const&pivots,matrix&b){ for(size_t i=size(pivots);i-- >0;){ b[i].normalize(); b[i] *=base(1)/A[i][pivots[i]]; for(size_t j=0;j<i;j++){ b[j].add_scaled(b[i],-A[j][pivots[i]]); } } } static void add_scaled_pair(vec_t&x,vec_t&y,vec_t const&p,vec_t const&q, std::array<base,4>c,size_t first=0){ if constexpr(requires{vec_t::add_scaled_pair(x,y,p,q,c,first);}){ vec_t::add_scaled_pair(x,y,p,q,c,first); }else{ x.add_scaled(p,c[0],first);x.add_scaled(q,c[1],first); y.add_scaled(p,c[2],first);y.add_scaled(q,c[3],first); } } template<gauss_mode mode,size_t count> void reduce_batch(size_t dst,size_t src,size_t lim){ static_assert(count<=8); std::array<typename vec_t::Base const*,count>sources; std::array<size_t,count>pivots; std::array<base,count>inverses; size_t first=lim; for(size_t t=0;t<count;t++){ sources[t] = &row(src+t); auto[p,inv] =row(src+t).find_pivot(); pivots[t] =p;inverses[t] =p<lim?inv:base(0); first=std::min(first,p); } if(first==lim)return; std::array<base,2*count>c{}; for(size_t r=0;r<2;r++)for(size_t t=0;t<count;t++){ if(pivots[t] >=lim)continue; base value=row(dst+r).normalize(pivots[t]); if constexpr(mode==normal){ uint64_t sum=value.getr(); for(size_t h=0;h<t;h++){ sum+=uint64_t(c[r*count+h].getr())*(*sources[h])[pivots[t]].getr(); } value.setr(sum%base::mod()); } c[r*count+t] = -value*inverses[t]; } vec_t::template add_scaled_batch<count,2>({&row(dst),&row(dst+1)},sources,c,first); } template<gauss_mode mode> void reduce_pair(size_t dst,size_t src,size_t lim){ auto&p=row(src),&q=row(src+1); auto[u,pu] =p.find_pivot(); auto[v,qv] =q.find_pivot(); if(u>=lim||v>=lim){ for(size_t j=dst;j<dst+2;j++){ if(u<lim)row(j).reduce_by(p); if(v<lim)row(j).reduce_by(q); } return; } auto mul= [](base x,base y){ if constexpr(impl::use_strassen<vec_t>){ x.setr(uint64_t(x.getr())*y.getr()%base::mod()); return x; }else return x*y; }; auto scales= [&](vec_t&a){ base s=mul(-a.normalize(u),pu); base t= -a.normalize(v); if constexpr(mode==normal)t-=mul(s,p[v]); t=mul(t,qv); return std::array{s,t}; }; auto a=scales(row(dst)),b=scales(row(dst+1)); add_scaled_pair(row(dst),row(dst+1),p,q, {a[0],a[1],b[0],b[1]},std::min(u,v)); } }; template<typename base_t> auto operator*(base_t t,matrix<base_t>const&A){return A*t;} }
#pragma GCC pop_options
#endif
